# Backend API base (no trailing slash). Same as main frontend NEXT_PUBLIC_API_BASE_URL target.
VITE_API_BASE_URL=http://localhost:8081/pet-ai-app

# Display name in welcome & placeholder (optional)
VITE_PET_NAME=小橘子
